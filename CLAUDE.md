# discord-challenge-bot

## Workflow

Before committing any task, provide manual testing instructions so the user can verify the feature works end-to-end before the commit is made. Skip testing instructions if the user explicitly asks to commit directly.
